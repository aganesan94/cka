# Introduction


# kube-proxy

* When installed via kubeadm runs as a daemonset
* Runs on all nodes
* Maintains network rules which allow for communication between pods
* Watches over services and endpoints within services